# Generic A/B OTA layout for boards with 4 MB flash.
#
# partitions_ota.csv gives each slot 4 MiB, which needs a 16 MB part. Here the
# whole flash is 4 MB, so the two slots are 1.9375 MiB each and consume the
# remainder exactly (0x20000 + 2 * 0x1F0000 = 0x400000).
#
# NVS stays at 0x9000/0x6000 to match every other Tesserae layout: a reflash
# that skips it keeps Wi-Fi credentials and the server registration, and the
# webflasher's offset-addressed `parts` rely on that address.
#
# Name,     Type, SubType,  Offset,    Size,     Flags
nvs,        data, nvs,      0x9000,    0x6000,
phy_init,   data, phy,      0xf000,    0x1000,
otadata,    data, ota,      0x10000,   0x2000,
ota_0,      app,  ota_0,    0x20000,   0x1F0000,
ota_1,      app,  ota_1,    0x210000,  0x1F0000,
